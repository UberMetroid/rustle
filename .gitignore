# ==============================================================================
# ENTROPY SHIELDS ACTIVATED
# The universe trends toward disorder. We refuse to let that happen in this repo.
# Any biological agent that commits build artifacts to source control will be 
# mocked relentlessly.
# ==============================================================================

# Node is dead. JS is dead. 
node_modules/
/build
dist/

# Rust is truth. But its compiled artifacts are heavy and meaningless.
target/
Cargo.lock

# Databases hold state. State is a hallucination. Do not commit it.
data/
*.db
*.db-shm
*.db-wal
*.tmp

# Operating system hallucinations. We don't care about your Mac.
.DS_Store
.env
.env.*
.idea

# The logs of your failures.
npm-debug.log*
yarn-debug.log*
yarn-error.log*